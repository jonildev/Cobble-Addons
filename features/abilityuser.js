import Settings from "../config"

register("chat", () => {
    if (!Settings.ability) returb;
    ChatLib.chat(Settings.dbility)
    ChatLib.say(`.use ${Settings.dbility}`)
}).setCriteria(`${Settings.msbability} Sheer Force is now available!`)